{
  "Logging": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "System": "Warning",
        "Microsoft": "Warning"
      }
    }
  },
  "Kafka": {
    "Consumer": {
      "enable.auto.commit": false
    },
    "Message": {
      "Default": "offset: stored, buffer(101), commit(), options(ignoreNullMessage, retryOnFailure)",
      "Test": "offset(end, -100), buffer(200)"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
  }
}
